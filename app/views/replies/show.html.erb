<% provide(:title, "ReadLetter") %>

<div class="reply replies-show">
  <div class="replies-show-item">
    <p>
      <span><%= @reply.dear %></span>
      <span><%= @reply.content %></span>
      <span><%=  @reply.from %><br>
      <%= @reply.created_at.strftime("%Y年%m月%d日 %H:%M") %></span>
    </p>
  </div>
</div>